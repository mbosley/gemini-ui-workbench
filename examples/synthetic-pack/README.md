# Synthetic pack

This fixture demonstrates the expected session shape for a Gemini implementation pack.

- session file: `.gemini-sessions/synthetic-demo.json`
- required file: `src/components/Hero.tsx`

Use it to validate:
- inspect-only contract parsing
- pack extraction and gating
- optional apply behavior
