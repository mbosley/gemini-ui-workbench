# Contributing

Keep contributions generic and reusable.

- Do not add private screenshots, private session files, or app-specific governance language.
- Prefer synthetic fixtures under `examples/`.
- Keep CLI behavior deterministic and covered by tests.
- Run `npm test` before opening a PR.
